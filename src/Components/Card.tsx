import React from "react";

interface Props {
  product: any;
}

const Card = ({ product }: Props) => {
  return (
    <div className="w-full h-[320px] rounded-xl shadow-cardshadow cursor-pointer">
      {}
    </div>
  );
};

export default Card;
